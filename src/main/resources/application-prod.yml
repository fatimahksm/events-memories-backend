app:
  storage:
    provider: r2
  scanning:
    enabled: true
  security:
    cookie-secure: true
spring:
  jpa:
    properties:
      hibernate:
        show_sql: false
logging:
  level:
    root: INFO
    org.springframework.security: WARN
